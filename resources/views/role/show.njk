{% extends 'base' %}

{% block navBarRole%}
    <div class="navbar-brand" >
        Admin
    </div>
{% endblock %}

{% block navSideBarList%}
    <li>
       {{ linkToAction('RoleController.index', '<i class="fa fa-fw fa-group"></i> User') }} 
    </li>
    <li class="active">
        {{ linkToAction('RoleController.index', '<i class="fa fa-fw fa-wrench"></i> Role') }} 
    </li>
    <li>
        {{ linkToAction('RoleController.index', '<i class="fa fa-fw fa-file"></i> Medicine Stock') }} 
    </li>
    <li>
        {{ linkToAction('RoleController.index', '<i class="fa fa-fw fa-medkit"></i> Sales Report') }} 
    </li>
{% endblock %}

{% block content %}
    <div class="col-lg-12">

        <div class="panel panel-default">

            <div class="panel-heading">
                <h3 class="panel-title">Role</h3>
            </div>

            <div class="panel-body">                                   
                 <div class="col-lg-12">

                    <form class="form-horizontal">
                         <div class="form-group">
                            <label class="col-sm-2 control-label">Role ID</label>
                            <div class="col-sm-10">
                                <p class="form-control-static">{{role.id}}</p>
                            </div>
                        </div>
                        <div class="form-group">
                            <label for="inputPassword" class="col-sm-2 control-label">Role Name</label>
                            <div class="col-sm-10">
                                <p class="form-control-static">{{role.name}}</p>
                            </div>
                        </div>
                    </form>
                    
                </div>      
            </div>

        </div>

        {{ form.open({ route: 'role.edit', params: {id: role.id} }) }}
            {{ form.button('Update','', { class: 'btn btn-success ',  style:'float:left;'}) }}
        {{ form.close() }}

        {{ form.open({ route: 'role.destroy', params: {id: role.id} }) }}
            {{csrfField}}
            {{ form.button('Delete','', { class: 'btn btn-danger ',style:'margin-left:20px' }) }} 
        {{ form.close() }}

    </div>

{% endblock %}